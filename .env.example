############################################################
# .env.example – Only keys actually read in Flutter code    #
# Firebase core config (API key, project id, etc.) is loaded #
# from android/google-services.json, NOT from this .env.     #
############################################################

# Cloudinary (required)
# CLOUDINARY_CLOUD_NAME and CLOUDINARY_UPLOAD_PRESET are the ONLY
# Cloudinary values the client reads. Use an UNSIGNED preset.
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_UPLOAD_PRESET=wishlist_unsigned

# Google Sign-In (server client ID) – used for proper OAuth flow / tokens
# Keep if Google Sign-In is enabled. If migrating away, leave blank.
GOOGLE_SIGN_IN_SERVER_CLIENT_ID=your-web-oauth-client-id.apps.googleusercontent.com

# (Optional) Deprecated / NOT read by app:
# FIREBASE_* values are ignored here (use google-services.json)
# CLOUDINARY_API_KEY / CLOUDINARY_API_SECRET must NEVER be in the client .env
# Remove any unused keys before committing your real .env.



