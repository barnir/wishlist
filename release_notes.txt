🚀 Wishlist App Beta v0.1.2 (Build 3)

✨ Novidades:
- Ícone de acesso rápido ao link (carrinho) sobreposto na grelha
- Abertura direta de links no browser (remoção do canLaunch + package visibility http/https)
- Filtro de categoria mais robusto (name + alias → whereIn) evitando itens “perdidos”
- Overlay de ação visual refinado para itens com link

🔧 Melhorias Técnicas:
- Suporte a variantes de categoria (alias) via `Category.storageCandidates()`
- Simplificação do fluxo de abertura de URL com sanitização centralizada
- Adição de intents http/https em `<queries>` para Android 11+
- Bump de versão para distribuição beta

🐞 Correções:
- Links que não abriam devido a `canLaunch` falso negativo
- Categoria com alias não filtrava (ex: "Saúde" vs "Saúde & Fitness")
- Ícone de link ausente na grelha

📌 Nota:
Se ainda não tiverem propagado índices antigos, a query fallback mantém UX. Testar abertura de vários domínios externos.

Pronto para distribuição beta. 🎯